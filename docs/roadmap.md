# Roadmap
| # | Done | Task                                 | Subtasks / Notes                                                                                                                                                                    |
| - |------| ------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 0 | [x]  | Create roadmap                       | Define architecture, milestones, API contracts, configuration strategy, and implementation plan                                                                                     |
| 1 | [x]  | Create service foundation            | Create service template, add gRPC Gateway for REST + gRPC APIs, add CLI interface                                                                                                   |
| 2 | [ ]  | Implement basic rate limit endpoints | Create endpoint accepting login/password/IP, return `200 OK` or `429 Too Many Requests`, add `Retry-After` and `X-RateLimit-*` headers, add `/clear-rate` endpoint                  |
| 3 | [ ]  | Implement pluggable rate limiter     | Create rate limiter interface, support configurable algorithms and limits, implement leaky bucket strategy                                                                          |
| 4 | [ ]  | Implement storage and access control | Support in-memory and key-value storage, add composite storage using in-memory cache + Redis, embed limiter into service, implement IP whitelist/blacklist and management endpoints |